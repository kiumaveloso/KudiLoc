# Mapbox GL JS access token (required for map)
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here

# KudiLoc API base URL (no trailing slash)
VITE_API_BASE_URL=https://your-api.onrender.com/api

# Optional: API key if your backend requires X-API-Key header
VITE_API_KEY=
